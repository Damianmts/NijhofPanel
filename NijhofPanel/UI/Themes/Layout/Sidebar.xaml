<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:services="clr-namespace:NijhofPanel.UI.Controls.Navigation">

    <!-- Sidebar NavButton -->
    <Style TargetType="{x:Type services:NavButton}">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type services:NavButton}">
                    <Border x:Name="back" CornerRadius="8" Padding="{TemplateBinding Padding}" Background="Transparent"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Path x:Name="icon" Height="25" Width="25" Stretch="Uniform" Fill="#046434"
                              Data="{TemplateBinding Icon}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#ffe560" />
                            <Setter Property="Fill" TargetName="icon" Value="#046434" />
                        </Trigger>

                        <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#ffe560" />
                            <Setter Property="Fill" TargetName="icon" Value="#046434" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar WindowButton -->
    <Style TargetType="{x:Type services:WindowButton}">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type services:WindowButton}">
                    <Border x:Name="back" CornerRadius="8" Padding="{TemplateBinding Padding}" Background="Transparent"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Path x:Name="icon" Height="25" Width="25" Stretch="Uniform" Fill="#046434"
                              Data="{TemplateBinding Icon}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#ffe560" />
                            <Setter Property="Fill" TargetName="icon" Value="#046434" />
                        </Trigger>

                        <DataTrigger Binding="{Binding IsWindowOpen, RelativeSource={RelativeSource Self}}"
                                     Value="True">
                            <Setter Property="Background" TargetName="back" Value="#ffe560" />
                            <Setter Property="Fill" TargetName="icon" Value="#046434" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>